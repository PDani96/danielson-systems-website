# Images

Placeholders in this directory render as plain labeled boxes so layout works before real assets exist. Replace them in place when ready.

| File | Used on | Notes |
|---|---|---|
| `headshot.svg` | about.html | Placeholder. Replace with `headshot.jpg` (or keep `.svg` and update markup). Target ~4:5 portrait crop, 600px+ on the long edge. |
| `logo.svg` | (not yet referenced) | Reserved for a future header mark. Add the `<img>` tag in the header of each HTML file when the logo exists. |

Past-employer logos are intentionally not used in v1.
